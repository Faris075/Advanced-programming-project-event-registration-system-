# ============================================================
# application-local.properties
#
# Local developer overrides – NEVER commit this file.
# Copy this example, rename to application-local.properties,
# and fill in your own values.
# ============================================================

DB_USERNAME=root
DB_PASSWORD=your_mysql_root_password

APP_SECRET=local-dev-secret-replace-me
# Mailtrap credentials go in .env (see .env.example or project root .env)
